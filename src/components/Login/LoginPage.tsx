import React from 'react';
import LoginReduxForm from "./LoginForm";

const LoginPage = () => {
    return (
        <div>
            <h1>LOGIN</h1>
            <LoginReduxForm/>
        </div>
    );
};

export default LoginPage;